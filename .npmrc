# Private Registry Configuration
# Use local Verdaccio for development (works both inside and outside monorepo)

# Shared packages
@large-event:registry=http://localhost:4873

# Team D packages
@teamd:registry=http://localhost:4873

# Alternative: GitHub Packages (for production or when Verdaccio is not available)
# @large-event:registry=https://npm.pkg.github.com
# @teamd:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${LARGE_EVENTS_GITHUB_TOKEN}

# React Native compatibility - hoist all dependencies to avoid multiple React copies
shamefully-hoist=true
node-linker=hoisted
